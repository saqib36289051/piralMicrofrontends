import { InlineConfig } from 'vite';
interface ViteConfig extends InlineConfig {
    debug: boolean;
    outFile: string;
    requireRef?: string;
}
export declare function runVite(options: ViteConfig): Promise<{
    bundle(): Promise<{
        outFile: string;
        outDir: string;
        name: string;
        requireRef: string;
    }>;
    onStart(cb: any): void;
    onEnd(cb: any): void;
}>;
export {};
